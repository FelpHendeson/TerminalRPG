const InterfaceUtils = require('./utils/interfaceUtils');

function start() {
    InterfaceUtils.drawBox([
        'TERMINAL RPG v2.0',
        'Uma Aventura Linear'
      ], 60);
}

start();